package ProjectOOP11.Parser;

import java.util.Map;

public class Opponent implements Expr{
    private Expr Opponent() throws SyntaxError{
        Expr v = null;
        return v;
    }

    @Override
    public long eval(Map<String, Long> bindings) throws EvalError {
        return 0;
    }
}
